<p>
  <strong>Activity:</strong>
	<%= @activity.activity_type %><br>
	<%= @activity.content%>
	<%= @activity.section.name%>
</p>


<%= form_for (@activity) do |f| %>
	
  <div class="container">
		<div class="col-lg-12">
			<div class="account-wall">
    		<h3><%= f.label :you_know_it_to_be_true %></h3>
				
				<%= f.fields_for :comments do |c| %>
					<%= c.text_area :context, size: "100x5", class: "form-control", required: "true", maxlength: 500, id: 'review_text' %>
			  <span id="counter" data-maximum-length="<%= @maximum_length %>"><%= @maximum_length %></span>
				<% end %>
				<div class="container">
					<br>
				</div>
				<div class="pull-right">
					<%= f.submit "Post", class: "btn btn-lg btn-danger", id: 'post_btn'%>
				</div>
				<div class="container">
					<br>
				</div>
			</div>
  	</div>
	</div>
<% end %>


 <table>
 	<thead>
		<tr>
			<th colspan="3"></th>
		</tr>
   </thead>
 
   <tbody>
     <% @comments.each do |comment| %>
     <tr>
 			<td><%= image_tag comment.user.avatar.url(:thumb) %></td>
 			<td></td>
 			<td>
 				<%= comment.user.first_name %><br/>
 				<%= comment.context %><br/>
 				<%= comment.created_at %>
 			</td>
 		</tr>
     <% end %>
   </tbody>
 </table>