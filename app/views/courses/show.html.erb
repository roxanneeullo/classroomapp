<p>
  <strong>Name:</strong>
  <%= @course.name %>
</p>
<table>	
	 <% @course.subjects.each do |subject| %>
	 <tr>
        <td><%= subject.name %></td>
			
					</tr>
		    <% end %>
</table>

<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>
