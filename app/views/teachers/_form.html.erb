<%= form_for(teacher) do |f| %>
  <% if teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(teacher.errors.count, "error") %> prohibited this teacher from being saved:</h2>

      <ul>
      <% teacher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	
	
	
		<div class=" col-md-9 col-lg-9 "> 
			<table class="table table-user-information">
				<tbody>
					<tr>
						<td> <%= f.label :user_id %></td>
	  				<td><%= f.collection_select(:user_id, User.order('username asc'), :id, :username, { :prompt => 'Select User' }, class: "form-control")  %></td>
					</tr>
					<tr>
				    <td><%= f.label :department_id %></td>
						<td><%= f.collection_select(:department_id, Department.order('name asc'), :id, :name, { :prompt => 'Select Department' }, class: "form-control")  %></td>
				  </tr>
					<tr>
						<td><%= f.label :status %></td>
						<td><%= f.select(:status, options_for_select(['Fulltime', 'Parttime']), 
		     				  {},class: "chzn-select form-control") %></td>
					</tr>
					<tr>
	    			<td><%= f.label :block_id %></td>
						<td><%= f.collection_select(:block_id, Block.order('name asc'), :id, :name, { :prompt => 'Select Block' }, class: "form-control")  %></td>
					</tr>
				</tbody>
			</table>
			<div class="actions">
	    	<%= f.submit "Update", class: "btn btn-md btn-danger pull-right" %>
	  	</div>
		</div>	
	<% end %>
	